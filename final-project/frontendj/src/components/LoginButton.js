import React from 'react';
import './LoginButton.css'; // Import the corresponding CSS file

const LoginButton = () => {
    return (
        <div className="login-container">
            <button className="login-button">Login</button>
        </div>
    );
};

export default LoginButton;
